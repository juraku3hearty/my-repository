# タイムカードOCR（100チャレ No.2 / 弁護士・未払給料案件）

タイムカード画像を Gemini で **2モデル読み＋論理チェック** して確認シートに出力する GAS。
確認シートを人が目視 → VBA で「給与第一」へ転記、という流れの「入口〜確認」部分。

## 構成

```
Drive「タイムカード＿未処理」(画像投入)
        │  processUnprocessedTimecards()
        ▼
① Flash で2回読む（gemini-3.5-flash×2）… 認識率が最も高いモデルで読む
② Pro で全件“再チェック”（gemini-2.5-pro）… Flashの値と食い違う所を炙り出す
③ 論理チェック … 曜日整合・出<退・中央値からの外れ値
        │  ①②③のどれかで怪しい所に色付け＋Pro再読メモ
        ▼
確認シート（タイムカードOCR）… 怪しいセルだけ人が目視・修正
        │
        ▼
VBA（給与第一.xlsm Module1）… 入力欄のみ・上書きなし
```

## セットアップ

1. **確認シートにスクリプトを設置**（コンテナバインド推奨）し、`Code.gs` を貼り付け。
2. **スクリプトプロパティ** に Gemini API キーを登録
   - `GEMINI_API_KEY` = `AIza...`（Google AI Studio で発行）
3. `CONFIG` を環境に合わせて調整
   - `SHEET_NAME` … 確認シートのシート名（既定 `シート1`）
   - `UNPROCESSED_FOLDER_ID` … `1VkjUeNaI4m4iwPMCZlm11uGhqBIMoSeE`（タイムカード＿未処理）
   - `CONFIRM_SHEET_ID` … `1aoLD-3WAQ8MoO1DMX_y49CObAKka_pxRcPQ_cOmV2l8`
   - `PROCESSED_FOLDER_ID` … 処理後に移動したい場合のみ設定
   - `CLOSING_DAY` … 締め日（このカードは `25`）
4. `processUnprocessedTimecards` を手動実行（初回は権限承認）。
   - 定期実行したい場合はトリガーを設定。

## 設計上の判断（重要）

- **読み取りは同一モデル(Flash)×2**：実地では Flash(gemini-3.5-flash) の認識率が最も
  高く、弱いモデルと突き合わせると誤検知が増えるだけ。だから読みは Flash で固める。
- **エラー検査は Pro で“全件”再読**：色付きセルだけ再読すると、Flash2回が同じ誤読で
  一致した“色の付かない誤り”を拾えない。Pro(gemini-2.5-pro)で全部読んで Flash と
  食い違うセルを炙り出す＝これが本当のエラーチェック（`checkAgainstPro_`）。
  Pro再読値はセルのメモに残し、値の自動上書きはしない。
  - 全件 Pro でもコストは1案件で数十円程度。
- **論理チェック**（2回が一致していても発火）
  - 曜日：カード印字の曜日 vs 日付から計算した曜日 → 不一致は要確認（行ズレ検出）
  - 出 < 退 でなければ異常
  - **中央値からの外れ値**：勤務時刻は時間帯が集中するので、中央値から
    `OUTLIER_MARGIN_MIN`(既定150分)以上ズレた打刻は誤読の疑いとして色付け
    （例：毎日6時台の出勤が1件だけ16時台＝桁誤りを検出）。
- **締め日25・X案**：カード「N月分」の 26〜31日は前月、1〜25日は当月として
  カレンダー実日付で記録（`toCalendarDate_`）。
- **列マッピング A案**：出勤打刻→午前IN(E列) / 退勤打刻→午後OUT(H列)。
- 最終的な正しさは **確認シートの人手チェック** が担保（AIは「埋める＋怪しい所に色」まで）。

## 色とメモの意味

- 🟥 赤：両方とも読めない（要入力）
- 🟡 黄：Flash 1回目と2回目で食い違い
- 🟧 橙：論理チェック（曜日/出退順序/外れ値）で矛盾、または Pro と食い違い
- セルのメモ「Pro再読: 6:16」：上位モデルの読み。確認の参考に。

## 関連ファイル

- `Code.gs` … OCR本体
- `導入手順_弁護士先生向け.md` … 先生に渡す導入・運用・費用の案内
